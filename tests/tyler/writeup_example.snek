(let ((x (vec false true 17)) (y (vec 1 2)))
     (block
        (set! x (vec nil y nil))
        (set! y nil)
        (let ((z (vec 1 2 3 4)))
            (block
                (print x)
                (print y)
                z
            )
        )
     )
)
